= TODO
* add zsh rpm package  and set default shell to zsh

== Base
* Monit should determine system name from hostname


== LAMP


== Rails
* build nginx rpm
* nginx install & setup scripts
* monit setup scripts
